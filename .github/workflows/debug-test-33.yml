name: debug issue 32 test

on:
  pull_request:
    paths:
      - 'scripts/test.sh'
      - '.github/workflows/debug-test-33.yml'

permissions:
  contents: read

jobs:
  trace:
    runs-on: macos-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          show-progress: false
      - name: Install JSON parser
        run: brew install jq
      - name: Trace repository test
        run: bash -x scripts/test.sh
